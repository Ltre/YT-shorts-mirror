D:\Workspace\github\Ltre\YT-shorts-mirror\server\download-adapter.js

帮我修改代码

函数里需要执行几个命令下载缓存到服务器。再按照代码注释里的提示补全代码
    1、先用```yt-dlp -F "https://url.url/urlurlu地址" --cookies "具体COOKIES文件路径" ``` 获取最高720P的码率参数
    2、再用```yt-dlp -f  "具体的码率参数，可用+号连接参数" "https://url.url/urlurlu地址" --cookies "具体COOKIES文件路径" ```
    
    

问：
    
    开发个管理后台：
    1、先支持yt cookies录入，保存到源码的data目录中，便于日后download-adapter.js调用
    2、管理服务器缓存的视频
    3、长辈浏览记录
    4、长辈的数据画像（这个先不开发，先写具体计划到 doc/devlogs 里）

答：

    我先快速摸一下项目结构和现有路由/前端形态，然后直接补管理后台；“数据画像”我会先落到 doc/devlogs 的计划文档里，不做功能实现。

    已运行 2 条命令

    看到工作区已有一些未提交改动（data 和 devlog、package-lock），我会只碰这次需要的文件，避免覆盖你现有内容。

    已运行 8 条命令

    现有页面是纯静态 PWA + server.js 原生 HTTP 路由，所以我会用同样风格加一个独立 /admin.html，后台接口挂在 /api/admin/...，这样不引入框架也不扰动老人端。

    已运行 2 条命令

    我会把 cookies 的默认调用路径也接到 data/yt-cookies.txt，这样后台保存后，之前的下载适配器能自然读到。

    已编辑 1 个文件

    后端接口已经补上：cookies 录入、缓存列表/删除/手动缓存、浏览记录查询。接下来我加后台页面和计划文档。

    已编辑 1 个文件

    现在后台保存的 data/yt-cookies.txt 会成为 yt-dlp 默认 cookies 文件；如果单个视频或环境变量指定了 cookies，仍然优先使用指定路径。

    已创建 4 个文件已运行 1 条命令

    后台页面和画像计划已加好。我现在跑语法检查，顺手看一下 diff 有没有明显问题。

    已运行 5 条命令

    语法检查通过。git diff --check 只报了既有 doc/prompts/devlog/dev-2607A.md 的尾随空格，不是这次新增文件；我不会去动它。

    已运行 2 条命令

    我再做一次只针对本轮改动里已跟踪文件的 diff check，避免被旧 devlog 噪音挡住判断。

    已运行 git diff --check -- server.js server\download-adapter.js

    已开发好管理后台，入口是：

    [public/admin.html](D:\Workspace\github\Ltre\YT-shorts-mirror\public\admin.html)

    完成内容：

    - YT cookies 录入
      - 后台保存到 `data/yt-cookies.txt`
      - [server/download-adapter.js](D:\Workspace\github\Ltre\YT-shorts-mirror\server\download-adapter.js) 默认改为读取该文件
      - 仍兼容 `video.cookiesPath` / `video.cookieFile` / `YT_DLP_COOKIES` 优先级

    - 服务器缓存视频管理
      - 查看所有视频缓存状态、文件大小、最近缓存任务
      - 支持手动加入缓存队列
      - 支持删除服务器缓存文件

    - 长辈浏览记录
      - 按 `elderId` 查询最近事件
      - 显示观看、喜欢、收藏、偏好标签摘要

    - 数据画像
      - 暂未开发功能
      - 已写计划到 [doc/devlogs/elder-profile-plan.md](D:\Workspace\github\Ltre\YT-shorts-mirror\doc\devlogs\elder-profile-plan.md)

    后端改动在：

    - [server.js](D:\Workspace\github\Ltre\YT-shorts-mirror\server.js)
    - [server/download-adapter.js](D:\Workspace\github\Ltre\YT-shorts-mirror\server\download-adapter.js)

    已验证：

    - `node --check server.js`
    - `node --check public\admin.js`
    - `node --check server\download-adapter.js`
    - 本轮已跟踪文件 `git diff --check` 通过

    注意：全量 `git diff --check` 仍会报你原来已有的 `doc/prompts/devlog/dev-2607A.md` 尾随空格，我没有动那部分。


问：
    1、由于本地开发，没有天然的国际互联网环境，是通过trojan-GFW进行代理上网的，我可以提供代理命令给你，你到代码里加上去。这样就能在本地缓存视频。
        export HTTP_PROXY=http://127.0.0.1:58591; export HTTPS_PROXY=http://127.0.0.1:58591; export ALL_PROXY=socks5://127.0.0.1:51837
        
答：
    可以，已加到 [package.json](D:\Workspace\github\Ltre\YT-shorts-mirror\package.json) 里了。

    现在直接用：

    ```bash
    npm run dev
    ```

    就会默认注入：

    ```bash
    YT_DLP_PROXY=http://127.0.0.1:58591
    YT_DLP_ALL_PROXY=socks5://127.0.0.1:51837
    ```

    另外保留了一个不走代理的启动方式：

    ```bash
    npm run dev:no-proxy
    ```

    我也同步更新了 [doc/devlogs/local-proxy.md](D:\Workspace\github\Ltre\YT-shorts-mirror\doc\devlogs\local-proxy.md)。

    已验证：

    - `package.json` JSON 解析通过
    - `server\download-adapter.js` 语法检查通过